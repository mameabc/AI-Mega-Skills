{
  "slug": "{{skill_slug}}",
  "name": "{{skill_name}}",
  "description": "{{skill_description}}",
  "domain": "{{domain_slug}}",
  "subdomain": "{{subdomain_slug}}",
  "version": "0.1.0",
  "owners": [
    "{{owner_handle}}"
  ],
  "tags": [
    "{{tag_slug}}"
  ],
  "inputs": [
    "{{required_input}}"
  ],
  "outputs": [
    "{{expected_output}}"
  ],
  "validation": [
    "{{validation_check}}"
  ],
  "license": "MIT",
  "status": "proposed"
}

